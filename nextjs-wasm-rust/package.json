{
  "dependencies": {
    "@mdx-js/loader": "^1.6.16",
    "@next/mdx": "^9.5.3",
    "@theme-ui/presets": "^0.3.0",
    "next": "latest",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "theme-ui": "^0.3.1"
  },
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "start": "next start",
    "deploy:vercel": "npm run build && npm run static-push && npm run clean:vercel",
    "static-push": "cp -r .vercel out/ && cd out && vercel --prod",
    "clean:vercel": "vercel rm --safe --yes zone"
  },
  "license": "MIT",
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.3.1",
    "standard": "^14.3.4"
  }
}
