{
  "dependencies": {
    "@mdx-js/loader": "^1.6.18",
    "@next/mdx": "^9.5.3",
    "@theme-ui/presets": "^0.3.0",
    "canvas": "^2.6.1",
    "next": "latest",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "theme-ui": "^0.3.1"
  },
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "start": "next start",
    "deploy:vercel": "npm run static && cd out && vercel --prod && npm run clean:vercel",
    "clean:vercel": "vercel rm --safe --yes zone",
    "preview:vercel": "npm run static && cd out && vercel",
    "static": "npm run build && cp -r .vercel out/"
  },
  "license": "MIT",
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.3.1",
    "standard": "^14.3.4"
  }
}
